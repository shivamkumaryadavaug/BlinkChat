import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.blinkchat.app',
  appName: 'BlinkChat',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
